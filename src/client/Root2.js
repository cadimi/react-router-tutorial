import { BrowserRouter } from 'react-router-dom'
import App from '../shared/App2';
import React from "react";

const Root = () => (
    <BrowserRouter>
        <App/>
    </BrowserRouter>
);

export default Root;
